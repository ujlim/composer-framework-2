version: 1

vine:
  vine_id: ujlim_gcs_merge_keep_source_sample
  description: Merge multiple GCS CSV files and keep source shards
  owner: framework-test
  schedule: null
  timezone: Asia/Seoul
  start_date: "2026-09-22"
  max_active_runs: 1
  dagrun_timeout_seconds: 7200
  tags: [sample, test, gcs]

business_date:
  type: daily

runtime:
  project_id: gcp-prod-edp-sa-ubi
  location: asia-northeast3
  gcp_conn_id: google_cloud_default
  impersonation_chain: null

grape_defaults:
  retries: 1
  retry_delay_seconds: 60
  execution_timeout_seconds: 7200
  pool: default_pool
  priority_weight: 1

grapes:
  - grape_id: merge_csv_keep_source
    type: gcs_merge_csv
    source:
      bucket: bucket-prod-sa-ubi-outbound-test-1
      prefix: "outbound/{{ ti.xcom_pull(task_ids='resolve_business_date')['business_date'] }}/"
      pattern: "*.csv"
    destination:
      object: "merged/outbound_{{ ti.xcom_pull(task_ids='resolve_business_date')['business_date'] }}.csv"
    options:
      csv_header: true
      csv_encoding: utf-8
      overwrite: true
      delete_source: false
      allow_empty: false

      # merged CSV의 데이터 row 수를 기록하는 FIN
      fin_filename: "merged/outbound_row_count_{{ ti.xcom_pull(task_ids='resolve_business_date')['business_date'] }}.fin"

      # GCS에 최종 생성된 데이터 파일 수를 기록하는 FIN (merge이므로 1)
      file_count_fin_filename: "merged/outbound_file_count_{{ ti.xcom_pull(task_ids='resolve_business_date')['business_date'] }}.fin"
